# Guía rápida: Deploy Firebase + React

## 📁 1. Asegurate de estar en la carpeta del proyecto

```bash
cd ruta/del/proyecto
# Ejemplo:
cd "F:\Proyectos web\jd-studio-web"

# Si hicistes cambios en tu React App, compila la version lista para produccion
npm run build

# Subí el sitio a Firebase
firebase deploy  #¡Listo! Ya está publicado.